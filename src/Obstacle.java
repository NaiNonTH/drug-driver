public class Obstacle extends Entity {
    public Obstacle(String name, int width, int height, boolean centered, int slot, int sceneWidth, int roadWidth) {
        super(name, width, height, centered, slot, sceneWidth, roadWidth);
    }
}
